[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "diamond-console"
version = "0.1.0"
description = "3-player Diamond tournament/operator console (PySide6 + QML)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "PySide6>=6.6",
    "qtawesome>=1.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
]
alphazero = [
    "torch>=2.5",
    "trueskill>=0.4.5,<0.5",
]

[project.scripts]
diamond = "diamond.main:main"
alphadiamond-train = "diamond.alphazero.orchestration.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
diamond = [
    "qml/*.qml",
    "qml/Style/*.qml",
    "qml/Style/qmldir",
    "assets/fonts/*.ttf",
    "assets/fonts/OFL.txt",
    "assets/sounds/*.m4a",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
markers = [
    "gui: needs PySide6 and a Qt platform plugin; deselect with -m 'not gui'",
]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
